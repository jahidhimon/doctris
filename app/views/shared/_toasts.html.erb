<%- if flash.any? %>
  <div class="toasts">
    <%- flash.each do |type, message| %>
      <div controller="toasts" class="alert alert-primary" role="alert">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
